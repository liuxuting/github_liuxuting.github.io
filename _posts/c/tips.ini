  1) 有符号整型的最小值INT_MNT
  2) 有符号整型的最大值INT_MAX
  3) c中!=表示不等于
  4) c if语法：
      if (布尔表达式) {
          /* 在布尔表达式为 true 时执行 */
      }
  5) c 循环语法：
      while (循环判断条件) {
          //循环执行语句
      }
  6) for循环：
      for (int i=0;i<n;++i) {
          ...
      }
  7) 字符串底层是数组
